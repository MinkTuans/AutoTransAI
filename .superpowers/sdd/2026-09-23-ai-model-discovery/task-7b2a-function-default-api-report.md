# Task 7B2A — canonical Function default write

Base `ac500ff`; implementation commit `b1f024d`. Implementation: `PUT /api/ai/functions/{function_id}` in a separate safe-write router. Body accepts only `model_id`, an exact `CatalogModel.id`; it derives and persists the provider ID from that row. It rejects unknown function/model, retired or disabled model/provider, incompatible capability, and unavailable key/access. Fully unknown capability remains selectable. Credential-scoped models require an enabled key and listing edge; Fal/ElevenLabs public-catalog candidates require an enabled provider key without claiming per-key entitlement; Edge TTS and system Pollinations/local-image use the existing router's keyless allowance. The result uses the canonical GET `FunctionView` shape and clears prior `configuration_error` while preserving legacy fallback columns.

Concurrency: the write locks the selected provider using the shared `catalog_revision` mutation and re-reads the model/provider before validation. Refresh and credential writers use that lock, so a concurrent refresh retirement or key change serializes against the selection; an in-flight older refresh becomes stale. The deterministic regression mutates retirement at the lock boundary and verifies a 409 with the previous config preserved. This is a boundary test rather than a live multi-connection timing test.

Red/green: 16 new API cases initially failed because the route module did not exist. After implementation, an isolated fixture needed provider-before-model insertion ordering; corrected fixture and reran to `16 passed`. Fresh guarded suite: `cd backend && ../.venv/bin/pytest -q tests/test_ai_function_default_api.py tests/test_ai_catalog_read_api.py tests/test_ai_key_api.py tests/test_ai_routing_contract.py tests/test_credential_service.py tests/test_model_refresh.py` → `136 passed` (28.12s). `git diff --check` passed.

All API tests use disposable SQLite, synthetic keys/models, and an outbound socket-connect guard. No real key/provider or production DB was used. Bad JSON/type and forbidden fallback/provider fields return a fixed validation response. An injected exception containing a synthetic secret is absent from response and captured logs. The new endpoint does not seed keyed models or alter GET, legacy routes, UI, migrations, or fallback selection. The full repository suite was outside this scoped, isolation-only checkpoint.
