# Task 7B2B — legacy Settings guard

Base `52fb5b3`; implementation commit `b7d27f7`. Three legacy Settings GET endpoints no longer call `SettingsService.ensure_defaults_seeded`. `GET /api/settings` still fills missing system values virtually; legacy Function/Model lists reflect stored rows and may be empty on a fresh database until Task 9 bootstrap/import. The explicit seeder remains callable, and no historical row was deleted.

Legacy `PUT /api/settings/functions/{function_id}` now rejects an existing active or retired `CatalogModel.id` default with a fixed 409 pointing to canonical `/api/ai/functions/{function_id}`. Its service locks the Function row with a current read before checking identity, so a concurrently committed canonical choice is not overwritten. Unmigrated legacy Function updates still use their prior payload and fallback behavior. Legacy `AIModel` PUT/DELETE remains separate from `CatalogModel`; DELETE also refuses to reassign a canonical Function default when IDs collide.

The legacy eligible-provider response shape is retained, but its `configured` flags now read enabled canonical `APIKey` rows. This avoids `KeyManager` first-use bootstrapping of plaintext `api_keys.json` during GET. Legacy-only keys may display as unconfigured until Task 9 imports them; Task 8 UI will consume canonical endpoints.

Red/green: the initial isolated suite had six expected failures (GET seeding/resurrection/image switch, canonical PUT overwrite, colliding model DELETE) and two passing compatibility checks. After route/service changes, the legacy tests and explicit seeder unit tests passed. A new regression that made access to the legacy key store raise failed before the read-path change; after querying enabled canonical keys, the suite passed. A MySQL SQL-compilation assertion for current `FOR UPDATE` Function reads failed for legacy model DELETE, then passed after the lock was added. Final guarded command: `cd backend && ../.venv/bin/pytest -q tests/test_legacy_settings_guard.py tests/unit/test_settings_and_stt.py tests/test_ai_function_default_api.py tests/test_ai_catalog_read_api.py tests/test_ai_key_api.py tests/test_model_refresh.py` → `102 passed` (27.31s). `git diff --check` passed.

All new tests use disposable SQLite, synthetic records, and an outbound socket guard. No real key, provider traffic, production DB, UI, or migration was used. The existing unisolated `tests/test_settings_api.py` suite was not run, because it uses the application's configured database and assumes GET seeding; Task 9 must revise its bootstrap expectations under an isolated fixture. Legacy provider/key writer removal remains outside this checkpoint.
