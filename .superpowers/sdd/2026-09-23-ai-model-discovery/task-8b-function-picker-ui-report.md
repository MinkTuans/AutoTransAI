# Task 8B — Canonical Function Model Picker report

Replaced only the Settings Function tab with `FunctionRouting`. It reads typed canonical Function rows, showing each configured provider, exact model ID, default status, and sanitized configuration issue without silently choosing a replacement. Its accessible Model Picker requests `/api/ai/models` with the Function capability, optional provider/query filters, and 25-row server pagination. Rows show provider and model IDs/names, capability evidence, active/retired/disabled state, available-key/listing scope, and default usage. `FULL_UNKNOWN` remains selectable; inactive, incompatible, and no-key rows have disabled Select controls. Public catalog candidates are labelled as entitlement-unverified. The shared allowlisted detail dialog exposes safe metadata.

Selection sends only `{model_id: CatalogModel.id}` to canonical `PUT /api/ai/functions/{id}`. A successful response updates the visible Function row; failure leaves its previous default unchanged and displays a fixed actionable message. No Function-tab fallback selector or free-text model input remains. Existing Models, Providers/Key Pool, Social, and System surfaces were preserved; legacy fallback database columns and backend behavior were not changed. The 8A catalog empty-provider message now appears only for an empty unfiltered provider, not a zero-result search.

TDD: the new component test failed before `FunctionRouting` existed, the Settings smoke test failed against the legacy Function tab, and the filtered-empty regression failed against the prior catalog wording. Final `npm test -- --run`: 3 files, 14 tests passed. Final `npm run build`: Vite 5.4.21 built 102 modules successfully. Tests mock `aiApi` and other external APIs; no real credentials, provider calls, production database, or backend writes were used. `git diff --check` is the final whitespace gate.

Known limits: UI availability is advisory; canonical PUT remains the authority against stale keys/models and race conditions. A public catalog listing does not prove per-key generation entitlement. No live browser/backend integration was performed in this offline UI slice. Task 8C will migrate the Providers/Key Pool tab.
